<thlogic>
    <attr sel="#headId"
          th:replace="~{fragments/head::headFragment}"/>
    <attr sel="#footerId"
          th:replace="~{fragments/footer::footerFragment}"/>

    <attr sel="#toLibrary"
          th:href="@{__${T(com.roadmap.springproject.util.LibraryMappings).LIBRARY}__}"
          th:text="#{message.enter}"/>
</thlogic>